% == == == == == == == == == == == == == == == == == == == == == == == == == ==
    == == == == == == == == == == == ==
    = % Appendix H : Risk Assessment % == == == == == == == == == == == == == ==
      == == == == == == == == == == == == == == == == == == == == == == == ==
    =

\subsection {
  Risk Assessment
}\label{app:risks}

The complete risk assessment for the STAR Robot project is presented below. This appendix provides detailed risk analysis including probability assessments, impact evaluations, and mitigation strategies for all identified project risks.

\subsubsection{
  Technical Risks
}

\begin{table}[H]
\centering
\caption { Technical Risk Register }
\label {
tab:
  technical_risks
}
\begin{tabular} {
  @{} p{3cm} p{2cm} p{2cm} p {
    6cm
  }
  @{}
}
\toprule
\textbf{Risk} & \textbf{Probability} & \textbf{Impact} & \textbf {
  Mitigation Strategy
}
\\
\midrule SLAM Performance Degradation & 3 & 4 & Test in varied environments;
implement fallback mapping modes \\
\addlinespace LiDAR Reliability Issues & 2 & 4 &
    Select industrial - grade sensor;
implement data filtering \\
\addlinespace Processing Resource Limits & 3 & 3 & Optimize algorithms;
consider compute offloading \\
\addlinespace 3D - Printed Gear Wear & 4 & 5 & Design with safety factors;
maintain spare parts inventory \\
\addlinespace Integration Complexity &Medium & High &Modular architecture;
comprehensive integration testing \\
\bottomrule
\end { tabular }
\end { table }

\subsubsection { Schedule Risks }

\begin{table}[H]
\centering
\caption { Schedule Risk Register }
\label {
tab:
  schedule_risks
}
\begin{tabular} {
  @{} p{3cm} p{2cm} p{2cm} p {
    6cm
  }
  @{}
}
\toprule
\textbf{Risk} & \textbf{Probability} & \textbf{Impact} & \textbf {
  Mitigation Strategy
}
\\
\midrule Component Lead Times & 4 & 3 & Order critical components early;
identify backup suppliers \\
\addlinespace Testing Delays & 3 & 3 & Build buffer time into schedule;
parallel testing where possible \\
\addlinespace Scope Creep & 2 & 4 & Strict change control;
prioritize core requirements \\
\bottomrule
\end { tabular }
\end { table }

\subsubsection { Resource Risks }

\begin{table}[H]
\centering
\caption { Resource Risk Register }
\label {
tab:
  resource_risks
}
\begin{tabular} {
  @{} p{3cm} p{2cm} p{2cm} p {
    6cm
  }
  @{}
}
\toprule
\textbf{Risk} & \textbf{Probability} & \textbf{Impact} & \textbf {
  Mitigation Strategy
}
\\
\midrule Budget Overrun & 2 & 3 & Track expenses closely;
maintain contingency reserve \\
\addlinespace Team Availability & 3 & 3 & Cross - train team members;
document all work \\
\addlinespace Equipment Failure & 2 & 4 & Maintain backup equipment;
regular maintenance schedule \\
\bottomrule
\end { tabular }
\end { table }
