#!/bin/sh
# Initialize configuration files for Unix Dev Container
mkdir -p "$HOME/.ssh" "$HOME/.gemini" "$HOME/.claude" "$HOME/.config/gh" "$HOME/.config/opencode" "$HOME/.config/openai"
touch "$HOME/.gitconfig" "$HOME/.claude.json"
