version: "1.0"
linter: qodana-cpp
profile:
  name: qodana.recommended

project-properties:
  qodana.compile.commands: build/compile_commands.json

include:
  - name: CheckDependencyLicenses

exclude:
  - name: All
    paths:
      - star-rx72n-firmware/lib/threadx
      - star-rx72n-firmware/lib/rx_nanopb/nanopb