# STAR Robot - Raspberry Pi 5 boot configuration
arm_64bit=1
kernel=Image
enable_uart=1
uart_2ndstage=1

# GPU memory (increased for camera processing)
gpu_mem=128

# Enable I2C, SPI
dtparam=i2c_arm=on
dtparam=spi=on

# Dual CSI Camera Support
# Enable both camera ports (CAM0 and CAM1)
dtoverlay=imx708,cam0
dtoverlay=imx708,cam1
camera_auto_detect=1

# HDMI output
hdmi_force_hotplug=1
